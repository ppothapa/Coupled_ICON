#!/bin/bash
set -eu
unset CDPATH
ulimit -c 0

SCRIPT_DIR=$(cd "$(dirname "$0")"; pwd)

export HK_GPU_MPIRUN_FLAGS=''
export HK_GPU_FCFLAGS='-acc=verystrict -gpu=cc80'
${SCRIPT_DIR}/hk.common.nvhpc --enable-gpu --enable-mpi-gpu "$@"
