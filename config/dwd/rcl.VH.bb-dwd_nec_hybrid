#!/bin/bash
#
# configure wrapper which is used for buildbot builder dwd_nec_hybrid (VH)
#
"$(dirname -- "${BASH_SOURCE[0]}")"/rcl.VH.gcc-9.2.0_mpi-2.22_art_oper --disable-les --enable-waves --enable-jsbach --disable-jsbach-hd "$@"
