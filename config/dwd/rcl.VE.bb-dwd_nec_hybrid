#!/bin/bash
#
# configure wrapper which is used for buildbot builder dwd_nec_hybrid (VE)
#
"$(dirname -- "${BASH_SOURCE[0]}")"/rcl.VE.nfort-4.0.0_mpi-2.22_art_oper --disable-les --enable-waves --enable-openmp "$@"
